{
  "name": "ocr-documents",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "backend:dev": "npm run dev --prefix backend",
    "backend:start": "npm start --prefix backend",
    "backend:extract": "npm run extract --prefix backend",
    "frontend:dev": "npm start --prefix frontend",
    "frontend:build": "npm run build --prefix frontend"
  }
}
